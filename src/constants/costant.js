const PURCHASE_UNIT = Object.freeze({
  UNIT_OF_LOTTO: 1000,
});

const LOTTO_CANSTANT = Object.freeze({
    MIN_LOTTO_NUMBER: 1,
    MAX_LOTTO_NUMBER: 45,
    LOTTO_LENGTH: 6
});

const CONSTANT = Object.freeze({PURCHASE_UNIT, LOTTO_CANSTANT});

export default CONSTANT;